import "package:flutter/material.dart";

class AppColors{
 AppColors._();

 static const Color darkBackgraund = Color(0xff020202);
 static const Color darkCard = Color(0xff19191A);
 static const Color darkText = Color(0xffffffff);

}